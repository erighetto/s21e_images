version: '3.4'

services:

  s21e_images:
    image: ${DOCKER_REGISTRY-}s21eimages
    build:
      context: .
      dockerfile: S21eImages/Dockerfile
    environment:
       - MYSQL_HOST
       - MYSQL_USER
       - MYSQL_PASSWORD
       - PROXY_11_KEY